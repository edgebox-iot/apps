FROM mattermost/focalboard:7.10.0
LABEL org.opencontainers.image.description="Focalboard is an open source project management tool. NOTE: upstream is unmaintained since 2023; this package is provided for existing users and defaults to the last known-good release."

EXPOSE 8000
